<main class="px-4 py-6 mx-auto max-w-7xl rounded-2xl border shadow-md border-slate-200">
    
    <section>
    <div class="flex flex-wrap justify-between items-center px-6 py-4 mb-6">
        <h2 class="text-xl font-semibold text-gray-800">Projects</h2>
        <div class="px-4 py-2 font-medium text-center text-white bg-indigo-400 rounded-lg border w-1/8 hover:bg-indigo-500">
            <%= link_to "Create Project" %>
        </div>
    </div>
    <%# Projects list header %>
    <div class="flex justify-between px-6 py-2 font-bold text-gray-800 bg-amber-50 rounded-xl border-2 border-slate-200">
        <div class="border">ID</div>
        <div class="border">Name</div>
        <div class="border">Actions</div>
    </div>
 </section>
 <%# Potential table structure %>
        <table class="w-full">
        <thead>
            <tr class="border border-gray-100">
                <th class="px-6 py-3 text-left">
                    <p class="font-medium text-gray-500">ID</p>
                </th>
                <th class="px-6 py-3 text-left">
                    <p class="font-medium text-gray-500">Name</p>
                </th>
                <th class="px-6 py-3 text-left">
                    <p class="font-medium text-gray-500">Actions</p>
                </th>
            </tr>
        </thead>
        <tbody>
            <tr class="border-t border-gray-100">
                <td></td>
            </tr>
        </tbody>
    </table>
</main>

<%# px-4 py-2 w-1/4  %>

<%# Old show.html.erb  %>
<section class="p-6 mx-auto mt-10 max-w-xl bg-white rounded shadow">
  <h1 class="mb-6 text-2xl font-bold text-gray-800">Project Details</h1>

  <div class="mb-6 space-y-2 text-gray-700">
    <p><span class="font-semibold">ID:</span> <%= @project.id %></p>
    <p><span class="font-semibold">Name:</span> <%= @project.name %></p>
  </div>

  <div class="flex flex-wrap gap-4 mb-6">
    <%= link_to "Edit", edit_project_path(@project), class: "px-4 py-2 bg-emerald-500 text-white rounded hover:bg-emerald-600" %>
    <%= link_to "Delete", project_path(@project), data: { confirm: "Are you sure?", turbo_method: :delete }, class: "px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600" %>
    <%= link_to "Back", projects_path, class: "px-4 py-2 bg-indigo-500 text-white rounded hover:bg-indigo-600" %>
  </div>

  <div class="flex flex-col">
    <%= link_to "View Sites", project_sites_path(@project), class: "inline-block text-emerald-600 hover:underline font-semibold" %>
    <%= link_to "View Samples", project_samples_path(@project), class: "inline-block text-emerald-600 hover:underline font-semibold" %>
  </div>
</section>
 
 <%# Old new.html.erb %>
 <section class="p-6 mx-auto mt-10 max-w-md bg-white rounded shadow-md">
  <h1 class="mb-6 text-2xl font-bold text-gray-800">New Project</h1>

  <%= form_with model: @project, local: true do |form| %>
    <div class="mb-4">
      <%= form.label :name, class: "block text-gray-700 font-semibold mb-2" %>
      <%= form.text_field :name, class: "w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-green-500" %>
    </div>

    <div>
      <%= form.submit "Create Project", class: "bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition" %>
    </div>
  <% end %>
</section>

<%# OLD SHOW_HTML_ERB for PROJECTS %>
nav_buttons: [
    Library::ButtonComponent.new(label: "View Sites", path: project_sites_path(@project), variant: :primary),
    Library::ButtonComponent.new(label: "View Samples", path: project_samples_path(@project), variant: :primary),
    Library::ButtonComponent.new(label: "Small Finds", path: project_small_finds_path(@project), variant: :primary)
  ],
  action_buttons: [
    Library::ButtonComponent.new(label: "Edit", path: edit_project_path(@project), variant: :secondary),
    Library::ButtonComponent.new(label: "Delete", path: project_path(@project), variant: :danger, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" })
  ],

  <%# index.html.erb for projects %>
    columns: [ 
    {
      label: "ID", 
      accessor: :id, 
    }, 
    {
      label: "Name", 
      accessor: :name,
    },
    {
      label: "Actions", 
      custom_render: true
    }
  ], 
  actions: [
    {
      label: "View", 
      path_proc: ->(project) {project_path(project)},
    
    }, 
    {
      label: "Edit", 
      path_proc: ->(project) {edit_project_path(project)},
    }, 
    {
      label: "Delete", 
      path_proc: ->(project) {project_path(project)},
      variant: :link_danger,
      data: { turbo_confirm: "Are you sure?", turbo_method: :delete }
    }
  ]