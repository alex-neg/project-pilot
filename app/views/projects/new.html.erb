<%= render Templates::FormTemplateComponent.new(
  breadcrumb: [
      {label: "Projects", path: projects_path},
      {label: "New"}
    ],
  header: {
    items: @projects,
    title: "Create New Project",
  },
  form: {
    items: Project.new,
    # parent: nil,
    pagy: @pagy,
    submit_label: "Create",
    inputs: [
      {
        label: "Name",
        accessor: :name
      }
    ],
  }
) %>