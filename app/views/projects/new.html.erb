<%= render Templates::FormTemplateComponent.new(
  model: @project,
  title: "New Project",
  actions: [
    {
      label: "Back", 
      path_proc: ->(_) {projects_path}
    }
  ], 
  fields: [:name]
) %>