<%= render Templates::FormTemplateComponent.new(
    breadcrumb: [
        {label: "Projects", path: user_projects_path(@user)},
        {label: "New"}
    ], 
    header: {
        title: "Crate New Project"
    }, 
    form: {
        record: @project,
        url: user_projects_path(@user),
        submit_label: "Create",
        inputs: [
            {
                label: "Name", 
                accessor: :name
            }, 
            {
                label: "Status", 
                accessor: :status,
                collection: Project.statuses.keys.map { |status| [status.titleize, status] }
            }
        ]
    }
)%>