<%= render Templates::FormTemplateComponent.new(
    breadcrumb: [
        {label: "Projects", path: user_projects_path(@user)},
        {label: "Edit"}
    ], 
    header: {
        title: "Edit Project"
    }, 
    form: {
        record: @project,
        url: user_project_path(@user, @project),
        method: :patch,
        submit_label: "Update",
        inputs: [
            {
                label: "Name", 
                accessor: :name
            }, 
            {
                label: "Status", 
                accessor: :status,
                collection: Project.statuses.keys.map { |status| [status.titleize, status] }
            }
        ]
    }
)%>
