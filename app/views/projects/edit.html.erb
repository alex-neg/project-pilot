<%=render Templates::FormTemplateComponent.new(
  model: @project,
  title: "Edit Project", 
  actions: [
    {
      label: "Back", 
      path_proc: ->(project) {project_path(project)}
    }
  ], 
  fields: [:name]
)%>