<%=render Templates::DetailTemplateComponent.new(
    title: @project.name,
    breadcrumb: [
        {label: "Users", path: users_path}, 
        {label: "#{@user.first_name} #{@user.last_name}", path: user_path(@user)},
        {label: "Projects", path: projects_path}, 
        {label: @project.name, path: project_path(@project)}
    ], 
    attributes: [
        {
            label: "ID", 
            value: @project.id
        },
        {
            label: "Name", 
            value: @project.name
        }, 
        {
            label: "Status", 
            value: @project.status
        }, 
        {
            label: "Sites", 
            path: project_sites_path(@project), 
            variant: :link,
            count: @project.sites.count
        }
    ], 
    actions: [
        {
            label: "Edit",
            path: edit_project_path(@project),
            variant: :secondary
        }, 
        {
            label: "Delete",
            path: project_path(@project),
            variant: :danger,
            data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
        }
    ]

)%>
