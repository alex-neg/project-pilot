<%=render Templates::FormTemplateComponent.new(
  parent: @project,
  model: @site,
  title: "Edit Site", 
  actions: [
    {
      label: "Back", 
      path_proc: ->(project) {project_sites_path(project)}
    }
  ], 
  fields: [:name]
)%>
