<%= render Templates::DetailTemplateComponent.new(
  model: @site,
  breadcrumb: [
      {label: "Projects", path: projects_path},
      { label: @project.name, path: project_path(@project)},
      {label: "Sites", path: project_sites_path(@project)},
      { label: @site.name, path: project_sites_path(@project)},
    ],
  action_buttons: [
    Library::ButtonComponent.new(label: "Edit", path: edit_project_site_path(@project, @site), variant: :secondary),
    Library::ButtonComponent.new(label: "Delete", path: project_site_path(@project, @site), variant: :danger, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" })
  ],
  show_picture: false
) %>