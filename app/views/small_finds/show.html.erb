<%= render Templates::DetailTemplateComponent.new(
  title: @small_find.name,
  breadcrumb: [
      {label: "Projects", path: projects_path},
      { label: @project.name, path: project_path(@project)},
      {label: "Small Finds", path: project_small_finds_path(@project)},
      {label: @small_find.name, path: project_small_finds_path(@project)}
    ],
  attributes: [
    {
      label: "Name",
      value: @small_find.name,
    },
    {
      label: "ID",
      value: @small_find.id
    },
    {
      label: "Description", 
      value: @small_find.description
    }, 
    {
      label: "Status/Type",
      value: @small_find.find_type
    }, 
    {
      label: "Image before",
      value: @small_find.before_photo
    },
    {
      label: "Image after",
      value: @small_find.after_photo
    }
  ],
  actions: [
    {
      label: "Edit",
      path: edit_project_small_find_path(@project, @small_find),
      variant: :secondary,
    },
    {
      label: "Delete",
      path: project_small_find_path(@project, @small_find),
      variant: :danger,
      data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
      }
    }
  ],
) %>