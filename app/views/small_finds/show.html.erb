<%= render Templates::DetailTemplateComponent.new(
  model: @small_find,
  breadcrumb: [
      {label: "Projects", path: projects_path},
      { label: @project.name, path: project_path(@project)},
      {label: "Small Finds", path: project_small_finds_path(@project)},
      {label: @small_find.name, path: project_small_finds_path(@project)}
    ],
  action_buttons: [
    Library::ButtonComponent.new(label: "Edit", path: edit_project_small_find_path(@project, @small_find), variant: :secondary),
    Library::ButtonComponent.new(label: "Delete", path: project_small_find_path(@project, @small_find), variant: :danger, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" })
  ],
  show_picture: true
) %>