<%=render Templates::DetailTemplateComponent.new(
    title: @excavation_unit.name, 
    breadcrumb: [
        {label: "Users", path: users_path}, 
        {label: "#{@user.first_name} #{@user.last_name}", path: user_path(@user)},
        {label: "Projects", path: user_projects_path(@user)}, 
        {label: "#{@project.name}", path: user_project_path(@user, @project)},
        {label: "Sites", path: user_project_sites_path(@user, @project)},
        {label: "#{@site.name}", path: user_project_site_path(@user, @project, @site)},
        {label: "Excavation Units", path: user_project_site_excavation_units_path(@user, @project, @site)},
        {label: "#{@excavation_unit.name}"}
    ],
    attributes: [
        {
            label: "ID",
            value: @excavation_unit.id
        }, 
        {
            label: "Name",
            value: @excavation_unit.name
        },
        {
            label: "Contexts",
            path: user_project_site_excavation_unit_path(@user, @project, @site),
            variant: :link,
            count: 0
        }
    ],
    actions: [
        {
            label: "Edit",
            path: edit_user_project_site_excavation_unit_path(@user, @project, @site, @excavation_unit),
            variant: :secondary,
        }, 
        {
            label: "Delete",
            path: edit_user_project_site_excavation_unit_path(@user, @project, @site, @excavation_unit),
            variant: :danger,
            data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
        }
    ]
)%>