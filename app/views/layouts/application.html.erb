<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Project Pilot" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <%= stylesheet_link_tag "application.css", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="overflow-hidden h-screen bg-slate-50">
    <div class="flex h-full">
      <!-- Sidebar -->
      <aside class="flex flex-col w-64 border-r backdrop-blur-md bg-white/80 border-slate-200/50">
        <!-- Logo -->
        <div class="p-6 border-b border-slate-200/50">
          <div class="flex items-center space-x-3">
            <div class="w-8 h-8 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-lg"></div>
            <h1 class="text-xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-slate-900 to-slate-600">
              Project Pilot
            </h1>
          </div>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 p-4 space-y-2">
          <a href="/projects" class="<%= 'bg-indigo-100 text-indigo-700 border-r-2 border-indigo-600' if request.path.start_with?('/projects') %> flex items-center space-x-3 px-4 py-3 text-slate-700 hover:bg-slate-100/70 rounded-xl transition-all hover:scale-105 group">
            <svg class="w-5 h-5 <%= 'text-indigo-600' if request.path.start_with?('/projects') %> text-slate-500 group-hover:text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
            </svg>
            <span class="font-medium">Projects</span>
          </a>
        </nav>

        <!-- User Profile -->
        <div class="p-4 border-t border-slate-200/50">
          <div class="flex items-center px-4 py-3 space-x-3 rounded-xl transition-all cursor-pointer hover:bg-slate-100/70">
            <div class="flex justify-center items-center w-8 h-8 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full">
              <span class="text-sm font-semibold text-white">JP</span>
            </div>
            <div class="flex-1">
              <p class="text-sm font-medium text-slate-700">John Pilot</p>
              <p class="text-xs text-slate-500">john@example.com</p>
            </div>
          </div>
        </div>
      </aside>

      <!-- Main Content Area -->
      <div class="flex overflow-hidden flex-col flex-1">

        <!-- Flash Messages -->
        <% if notice %>
          <div class="p-4 mx-6 mt-4 rounded-xl border border-emerald-200 backdrop-blur-sm bg-emerald-50/50">
            <p class="font-medium text-emerald-800"><%= notice %></p>
          </div>
        <% end %>

        <% if alert %>
          <div class="p-4 mx-6 mt-4 rounded-xl border border-red-200 backdrop-blur-sm bg-red-50/50">
            <p class="font-medium text-red-800"><%= alert %></p>
          </div>
        <% end %>

        <!-- Main Content -->
        <main class="overflow-auto flex-1 p-6">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>