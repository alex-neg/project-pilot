<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Project Pilot" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <%= stylesheet_link_tag "application.css", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-slate-50 h-screen overflow-hidden">
    <div class="flex h-full">
      <!-- Sidebar -->
      <aside class="w-64 bg-white/80 backdrop-blur-md border-r border-slate-200/50 flex flex-col">
        <!-- Logo -->
        <div class="p-6 border-b border-slate-200/50">
          <div class="flex items-center space-x-3">
            <div class="w-8 h-8 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-lg"></div>
            <h1 class="text-xl font-bold bg-gradient-to-r from-slate-900 to-slate-600 bg-clip-text text-transparent">
              Project Pilot
            </h1>
          </div>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 p-4 space-y-2">
          <a href="/projects" class="<%= 'bg-indigo-100 text-indigo-700 border-r-2 border-indigo-600' if request.path.start_with?('/projects') %> flex items-center space-x-3 px-4 py-3 text-slate-700 hover:bg-slate-100/70 rounded-xl transition-all hover:scale-105 group">
            <svg class="w-5 h-5 <%= 'text-indigo-600' if request.path.start_with?('/projects') %> text-slate-500 group-hover:text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/>
            </svg>
            <span class="font-medium">Projects</span>
          </a>
        </nav>

        <!-- User Profile -->
        <div class="p-4 border-t border-slate-200/50">
          <div class="flex items-center space-x-3 px-4 py-3 rounded-xl hover:bg-slate-100/70 transition-all cursor-pointer">
            <div class="w-8 h-8 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full flex items-center justify-center">
              <span class="text-white text-sm font-semibold">JP</span>
            </div>
            <div class="flex-1">
              <p class="text-sm font-medium text-slate-700">John Pilot</p>
              <p class="text-xs text-slate-500">john@example.com</p>
            </div>
          </div>
        </div>
      </aside>

      <!-- Main Content Area -->
      <div class="flex-1 flex flex-col overflow-hidden">

        <!-- Flash Messages -->
        <% if notice %>
          <div class="bg-emerald-50/50 border border-emerald-200 rounded-xl p-4 mx-6 mt-4 backdrop-blur-sm">
            <p class="text-emerald-800 font-medium"><%= notice %></p>
          </div>
        <% end %>

        <% if alert %>
          <div class="bg-red-50/50 border border-red-200 rounded-xl p-4 mx-6 mt-4 backdrop-blur-sm">
            <p class="text-red-800 font-medium"><%= alert %></p>
          </div>
        <% end %>

        <!-- Main Content -->
        <main class="flex-1 overflow-auto p-6">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>