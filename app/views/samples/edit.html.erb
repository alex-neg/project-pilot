<%=render Templates::FormTemplateComponent.new(
  breadcrumb: [
      {label: "Projects", path: projects_path},
      { label: @project.name, path: project_path(@project)},
      {label: "Samples", path: project_samples_path(@project)},
      {label: "Edit"}
    ],
  header: {
    title: "Edit Sample",
  },
  form: {
    record: @sample,
    url: project_sample_path,
    submit_label: "Update",
    inputs: [
      {
        label: "Name",
        accessor: :name
      },
      {
        label: "Description",
        accessor: :description
      },
      {
        label: "Type",
        type: :select,
        accessor: :sample_type,
        collection: Sample.sample_types.keys.map { |key| [key.titleize, key] }
      },
      {
        label: "Image",
        accessor: :photo
      }
    ],
  }
)%>